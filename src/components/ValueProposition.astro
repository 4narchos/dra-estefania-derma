---
---

<section class="value-proposition" id="filosofia">
  <div class="value-proposition-inner">
    <h2 class="value-proposition-title">
      Tu piel te habla. <em>Yo te ayudo a entenderla.</em>
    </h2>
    <p class="value-proposition-body">
      Cada piel cuenta una historia. Mi enfoque combina ciencia dermatológica, escucha activa y resultados naturales para que te sientas bien contigo misma.
    </p>
  </div>
</section>

<style>
  .value-proposition {
    padding: var(--space-20) var(--space-6);
    background: var(--color-midnight);
    color: var(--color-text-on-dark);
    text-align: center;
  }

  .value-proposition-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .value-proposition-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(var(--text-3xl), 6vw, var(--text-5xl));
    font-weight: 600;
    line-height: var(--leading-tight);
    margin-bottom: var(--space-8);
  }

  .value-proposition-title em {
    font-style: italic;
    color: var(--color-accent);
  }

  .value-proposition-body {
    font-size: clamp(var(--text-lg), 2vw, var(--text-xl));
    max-width: 48rem;
    margin: 0 auto;
    line-height: var(--leading-relaxed);
    color: rgba(245, 245, 240, 0.85);
  }
</style>
