---
interface Props {
  src: string;
  title?: string;
  compact?: boolean;
}

const { src, title = "Ubicación en Google Maps", compact = false } = Astro.props;
---

<div class:list={["map-widget", { "map-widget--compact": compact }]}>
  <iframe
    src={src}
    title={title}
    width="100%"
    height="100%"
    style="border:0;"
    allowfullscreen
    loading="lazy"
    referrerpolicy="no-referrer-when-downgrade"
  ></iframe>
</div>

<style>
  .map-widget {
    width: 100%;
    aspect-ratio: 4 / 3;
    min-height: 280px;
    border-radius: var(--radius-md, 12px);
    overflow: hidden;
    border: 1px solid var(--color-border-strong, #DACE97);
    background: var(--color-surface, #fff);
  }

  .map-widget--compact {
    aspect-ratio: 1 / 1;
    min-height: 200px;
  }

  .map-widget iframe {
    width: 100%;
    height: 100%;
    border: 0;
    display: block;
  }

  @media (min-width: 640px) {
    .map-widget {
      aspect-ratio: 16 / 9;
      min-height: 360px;
    }

    .map-widget--compact {
      aspect-ratio: 4 / 3;
      min-height: 220px;
    }
  }
</style>
